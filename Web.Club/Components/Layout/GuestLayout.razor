@inherits LayoutComponentBase
@inject NavigationManager Navigation

<RadzenComponents />
<RadzenNotification />
<RadzenDialog />
<RadzenContextMenu />
<RadzenTooltip />

<RadzenLayout>
    <RadzenHeader>
        <RadzenStack Style="height: 100%;padding: 10px;"  Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center"  JustifyContent="JustifyContent.SpaceBetween"  >
            <RadzenButton 
                          Variant="Variant.Text"
                          Icon="travel_explore"
                          Text="Astronomy Club Albattani"
                          Click="@(() => Navigation.NavigateTo("/"))" />

            <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" Gap="0.5rem">
                <RadzenAppearanceToggle />
                <AuthorizeView>

                    <Authorized>
                        <RadzenButton ButtonStyle="ButtonStyle.Primary"
                              Size="ButtonSize.Small"
                              Icon="home"
                              Text="Dashboard"
                              Click="@(() => Navigation.NavigateTo("/dashboard"))" />
                    </Authorized>
                    <NotAuthorized>
                        <RadzenButton ButtonStyle="ButtonStyle.Primary"
                              Size="ButtonSize.Small"
                              Icon="login"
                              Text="Sign in"
                              Click="@(() => Navigation.NavigateTo("/account/login"))" />
                    </NotAuthorized>
                </AuthorizeView>
                
            </RadzenStack>
        </RadzenStack>
    </RadzenHeader>

    <RadzenBody >
        <main>
            @Body
        </main>
    </RadzenBody>
</RadzenLayout>
